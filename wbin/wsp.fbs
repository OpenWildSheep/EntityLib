include "wbin.fbs";

namespace WBIN;

table SeedPatch
{
	aabb:AABB;
	position:Float3Channel (required);
	edgeVisibility:[Bool3] (required);
	color:Float3Channel;
	sourceFileInf:SourceFileInf;
}

root_type SeedPatch;
