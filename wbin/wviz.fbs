include "wbin.fbs";

namespace WBIN;

table Viz
{
	aabb:AABB;
	meshData:Float3Channel (required);
	normal:Float3Channel;
	colorChannelList:Float4ChannelList;
	materialBank:MaterialBank (required);
	materials:Materials (required);
	skin:Skin;
	sourceFileInf:SourceFileInf (required);
}

root_type Viz;
